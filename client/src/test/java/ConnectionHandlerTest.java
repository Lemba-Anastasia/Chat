import junit.framework.TestCase;

import java.io.InputStream;

import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

/**
 * Created by Lemba on 18.09.2018.
 */
public class ConnectionHandlerTest extends TestCase {
    /*public void testStartListening() throws Exception {
        final Client client = mock(Client.class);
        InputStream inputStream = mock(InputStream.class);
        when(inputStream.read()).thenReturn((int) '/').thenReturn((int) 'c').thenReturn((int) 'l').thenReturn((int) 'o').thenReturn((int) 's').thenReturn((int) 'e').thenReturn(0);
        when(client.getIn()).thenReturn(inputStream);
        ConnectionHandler connectionHandler = new ConnectionHandler(client);
        connectionHandler.startListening();
    }*/
}